<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_move_form_inh_pos_extend" model="ir.ui.view">
        <field name="name">account.move.form.inherit.xas.pos.extend</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <!-- Agregamos la condición a al linea de factura -->
            <xpath expr="//form[1]/sheet[1]/notebook[1]/page[@name='invoice_tab']/field[@name='invoice_line_ids']/tree" position="inside">
                <field name="xas_sale_condition_state_id" optional="show"/>
            </xpath>

        </field>
    </record>

</odoo>