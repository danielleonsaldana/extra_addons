<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Visibilidad restringida a la compañía ACTIVA -->
    <record id="pricelist_mla_rule_active_company" model="ir.rule">
        <field name="name">Pricelist MLA – solo compañía activa</field>
        <field name="model_id" ref="model_product_pricelist_mla"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
    </record>
</odoo>