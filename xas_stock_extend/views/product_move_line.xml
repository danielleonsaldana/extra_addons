<odoo>
    <record id="view_stock_move_line_tree_inherit" model="ir.ui.view">
        <field name="name">stock.move.line.tree.inherit</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree"/>
        <field name="arch" type="xml">
            <tree>
                <field name="fecha_efectiva" string="Fecha Efectiva"/>
                <field name="dias_trascurridos" string="Días Transcurridos"/>
                <field name="tipo_movimiento" string="Tipo de Movimiento"/>
                <field name="cantidad_neta" string="Cantidad Neta" />
            </tree>
        </field>
    </record>
</odoo>

