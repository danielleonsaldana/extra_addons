<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <!-- Plantilla de correo para Seguimiento de viaje -->
        <record id="mail_template_tracking_notification" model="mail.template">
            <field name="name">Seguimiento de viaje</field>
            <field name="model_id" ref="xas_tracking.model_tracking"/>
            <field name="email_from">{{ object.company_id.email_formatted or user.email_formatted }}</field>
            <field name="email_to">{{ object.xas_customs_agent_id.email_formatted }}</field>
            <field name="partner_to">{{ object.xas_customs_agent_id.id }}</field>
            <field name="subject">Notificación de seguimiento: {{object.name}}</field>
            <field name="description">Plantilla de correo para enviar al agente aduanal en seguimientos</field>
            <field name="body_html" type="html">
                <p>Hola,</p>
                <p>Te informamos tu asignación al viaje:</p>

                    <strong>ID:</strong> <span style="font-weight:bold;" t-out="object.xas_trip_number.name or ''"/>

                <br/><br/>
                <p>Saludos cordiales,</p>
                <t t-if="not is_html_empty(user.signature)">
                    <br/><br/>
                    <t t-out="user.signature or ''">--<br/>Mitchell Admin</t>
                </t>
            </field>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>